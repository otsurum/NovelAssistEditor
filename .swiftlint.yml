included:
  - AppCore
  - AppFeature
  - Persistance
  - NovelAssistEditor

excluded:
  - .build
  - DerivedData

disabled_rules:
  - trailing_whitespace
  - line_length

opt_in_rules:
  - empty_count
