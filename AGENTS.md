# NovelAssistEditor 開発ルール

このリポジトリは、macOS向けの小説執筆支援アプリです。変更は既存の責務分離とテスト容易性を保つことを優先します。

## 作業開始時

- まず `README.md` と、変更対象に最も近い `AGENTS.md` を読む。
- 実装フローや既存パターンを確認する必要がある場合は `SKILLS.md` も参照する。
- 既存の未コミット変更を消去・上書きしない。変更範囲を依頼内容に限定する。
- 不明な仕様は、既存コード・テスト・公開APIから推測し、推測で外部仕様を追加しない。

## アーキテクチャ

依存方向は常に次の一方向に保つ。

`AppFeature` → `Persistance` → `AppCore`

- `AppCore`: SwiftUI、SwiftData、TCAに依存しないドメインモデルとルール。
- `Persistance`: SwiftDataのEntity、Mapper、Client。`AppCore`の型を永続化する。
- `AppFeature`: TCAのReducer/State/ActionとSwiftUI View。データアクセスはClient経由にする。
- 上位層の都合を理由に下位層へUI・永続化の依存を持ち込まない。
- 新しい責務が既存レイヤーに収まらない場合は、依存方向を壊す前に構成案を整理する。

## 実装ルール

- 公開する型・プロパティ・イニシャライザには、既存コードに合わせて `public` を付ける。
- ドメインモデルは可能な範囲で `Equatable` と `Sendable` に準拠させる。
- SwiftDataのEntityとAppCoreのModelを直接混在させず、Mapperで変換する。
- Featureの非同期処理はTCAのDependencyとEffectで扱い、Viewから直接データ層を呼ばない。
- 既存の命名（`Feature`、`Client`、`Entity`、`Mapper`）とファイル配置に従う。
- 自動保存・削除・外部コマンドなど副作用を伴う処理は、失敗を状態へ反映し、ユーザーが結果を確認できるようにする。

## 秘密情報とユーザーデータ

- APIキー、アクセストークン、パスワード、秘密鍵、署名証明書、Cookieなどをソースコード、テスト、Fixture、ログ、スクリーンショット、生成物へ書き込まない。
- ローカル開発で秘密情報が必要な場合は、環境変数・Keychain・Git管理外の設定を使い、共有用ファイルには無効なプレースホルダーだけを置く。
- 秘密情報を扱う変更では、`.gitignore`、ログのマスキング、最小権限、秘密情報がエラーメッセージへ混入しないことを確認する。
- 作品本文、登場人物情報、設定資料などのユーザーデータを、依頼・仕様上必要な場合を除いて外部サービスへ送信しない。テストでは合成データを使う。
- 既に存在する秘密情報を見つけた場合は、値を表示・複製・コミットせず、影響範囲を最小化したうえでユーザーに失効・ローテーションを報告する。Git履歴の書き換えは明示的な依頼なしに行わない。

## Code Review Rules

- レビュー前に実際の差分と変更ファイル一覧を確認し、依頼外の変更、秘密情報の混入、生成物の追加を指摘する。
- 仕様変更、公開API変更、データモデル変更には、必要なテスト・Migration・ドキュメントがあるか確認する。
- `AppCore` の依存方向違反、Viewからの直接的なデータアクセス、Reducer以外へのビジネスロジック配置、エラーの握りつぶしを指摘する。安全な代替は、既存のClient・Dependency・Mapper・Reducerへ責務を戻すこと。
- 削除、自動保存、外部コマンド、ファイル変更など不可逆または影響の大きい処理では、明示的な確認、失敗時の復旧、キャンセル処理があるか確認する。
- 非同期処理では、MainActor境界、キャンセル、競合による古い状態の上書き、失敗の画面反映を確認する。
- 指摘には重要度と再現条件を添え、修正案は依頼範囲内の最小変更にする。問題がなければ、問題がないことを明確に報告する。
- フォーマットやLintだけの指摘はレビュー上の問題として扱わず、CIまたは検証結果で確認する。

## 検証

変更後は、変更内容に応じて次を実行する。

1. 該当するSwift Packageのテスト
2. `make fmt`
3. `make lint`
4. Xcodeプロジェクトに関係する変更では、可能な範囲でアプリまたは該当Schemeをビルドする

テストやビルドを実行できない場合は、理由を最終報告に明記する。テストを通すためだけに本番コードの挙動を弱めない。

## 完了条件

- 依頼された範囲だけが変更されている。
- 関連テストが追加または更新されている（テスト不要と判断した場合は理由がある）。
- フォーマット・Lint・テスト結果を確認できる。
- 最終報告には、変更点と検証結果を簡潔に記載する。
